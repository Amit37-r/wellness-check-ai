# wellness-check-ai
AI Wellness Tips App is a full-stack project built with React (Vite) on the frontend and Express on the backend. It generates personalized health and wellness tips based on a user’s profile (age, gender, goal). Users can:  Generate multiple tips,  Expand each tip into a detailed 3-step plan,  Save/unsave favorites (stored in localStorage).
